# Container benchmark profile.
# These values are intentionally fixed so benchmark runs are comparable.

BENCH_THREADS=2
BENCH_WARMUP_OPS=100000
BENCH_MEASURED_OPS=500000
BENCH_KEY_SPACE=100000
BENCH_LIST_KEY_SPACE=10000
BENCH_VALUE_SIZE=32
BENCH_BLOCKING_PAIRS=2
BENCH_BLOCKING_OPS=2000

#THREAD_POOL_SIZE=4
#CONCURRENCY_STRATEGY=THREAD_POOL